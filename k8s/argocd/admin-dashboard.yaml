apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: admin-dashboard
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/Mtceel/admin-dashboard.git
    targetRevision: main
    path: k8s
  destination:
    server: https://kubernetes.default.svc
    namespace: platform-services
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
      allowEmpty: false
    syncOptions:
    - CreateNamespace=true
